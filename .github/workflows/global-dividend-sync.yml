# SITE_CLOSED — entire workflow gated. Delete this comment and any `if: false  # SITE_CLOSED` lines to restore.
# DISABLED â€” PSX-only product focus. Re-enable when global markets return.

name: Global Dividend Sync

on:
  workflow_dispatch:

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled â€” PSX-only mode"
