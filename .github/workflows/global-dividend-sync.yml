# DISABLED — PSX-only product focus. Re-enable when global markets return.

name: Global Dividend Sync

on:
  workflow_dispatch:

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled — PSX-only mode"
