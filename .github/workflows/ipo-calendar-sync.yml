# DISABLED — PSX-only product focus. PSX IPO data is curated in data/ipos/psx_ipos.json.

name: IPO Calendar Sync

on:
  workflow_dispatch:

jobs:
  sync:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled — PSX-only mode"
