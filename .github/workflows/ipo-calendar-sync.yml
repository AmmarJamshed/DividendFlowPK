# SITE_CLOSED — entire workflow gated. Delete this comment and any `if: false  # SITE_CLOSED` lines to restore.
# DISABLED â€” PSX-only product focus. PSX IPO data is curated in data/ipos/psx_ipos.json.

name: IPO Calendar Sync

on:
  workflow_dispatch:

jobs:
  sync:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled â€” PSX-only mode"
